apiVersion: v1
kind: Service
metadata:
  name: aws-medilearn-service
  namespace: aws-medilearn
spec:
  type: NodePort
  selector:
    app: aws-medilearn
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007